@model IEnumerable<Tumin.Models.ApplicationUser>

<div class="container ">
    <h2>Usuarios</h2>

    <div class="row">

        <div class="col-md-12">
            <table class="table">
                <tr>
                    <th>
                        User
                    </th>
                    @*<th>
                            Rol
                        </th>*@
                    <th>
                        <a asp-action="@ViewBag.RedirectCreate">Registrar</a>
                    </th>
                </tr>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Email)
                        </td>
                        @*<td style="max-width:400px">
                                @{

                                    string roles = "";
                                    foreach (var rol in item.Roles)
                                    {
                                        roles = roles + rol;
                                    }
                                }
                                @roles
                            </td>*@

                        <td>
                            <a asp-action="Details" asp-controller="UserInformation"  asp-route-id="@item.Id" class="btn btn-info btn-simple btn-xs"><i class="fa fa-info-circle"></i></a>
                            <a asp-action="DeleteUser" asp-route-id="@item.Id" class="btn btn-danger btn-simple btn-xs"><i class="fa fa-trash"></i></a>
                            @*<a asp-action="EditUser" class="btn btn-info btn-simple btn-xs"><i class="fa fa-edit"></i></a>*@

                        </td>
                            </tr>
                        }


                    </table>

                </div>
            </div>
        </div>
